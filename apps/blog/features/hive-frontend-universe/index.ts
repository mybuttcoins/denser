/**
 * Hive Frontend Universe — public entry point.
 *
 * The default export is the module root (age gate → map). Import this path from
 * a `dynamic()` call so the heavy canvas code is code-split and only loads when
 * a player opens the game.
 */

export { default } from './hive-frontend-universe';
